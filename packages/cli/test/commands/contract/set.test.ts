// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('contract:set', () => {
  // it('runs contract:set cmd', async () => {
  //   const {stdout} = await runCommand('contract:set')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs contract:set --name oclif', async () => {
  //   const {stdout} = await runCommand('contract:set --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

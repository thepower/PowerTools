// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('contract:deploy', () => {
  // it('runs contract:deploy cmd', async () => {
  //   const {stdout} = await runCommand('contract:deploy')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs contract:deploy --name oclif', async () => {
  //   const {stdout} = await runCommand('contract:deploy --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

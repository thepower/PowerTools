// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('contract:get', () => {
  // it('runs contract:get cmd', async () => {
  //   const {stdout} = await runCommand('contract:get')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs contract:get --name oclif', async () => {
  //   const {stdout} = await runCommand('contract:get --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

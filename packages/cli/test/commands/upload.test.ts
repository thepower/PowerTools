// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('upload', () => {
  // it('runs upload cmd', async () => {
  //   const {stdout} = await runCommand('upload')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs upload --name oclif', async () => {
  //   const {stdout} = await runCommand('upload --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('acc:register', () => {
  // it('runs acc:register cmd', async () => {
  //   const {stdout} = await runCommand('acc:register')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs acc:register --name oclif', async () => {
  //   const {stdout} = await runCommand('acc:register --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

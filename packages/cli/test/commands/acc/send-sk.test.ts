// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('acc:send-sk', () => {
  // it('runs acc:send-sk cmd', async () => {
  //   const {stdout} = await runCommand('acc:send-sk')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs acc:send-sk --name oclif', async () => {
  //   const {stdout} = await runCommand('acc:send-sk --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

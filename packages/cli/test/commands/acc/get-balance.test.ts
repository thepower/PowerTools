// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('acc:get-balance', () => {
  // it('runs acc:get-balance cmd', async () => {
  //   const {stdout} = await runCommand('acc:get-balance')
  //   expect(stdout).to.contain('hello world')
  // })
  // it('runs acc:get-balance --name oclif', async () => {
  //   const {stdout} = await runCommand('acc:get-balance --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});

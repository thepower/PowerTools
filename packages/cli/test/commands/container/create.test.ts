// import {runCommand} from '@oclif/test'
// import {expect} from 'chai'

describe('container:create', () => {
  // it('runs container:create cmd', async () => {
  //   const {stdout} = await runCommand('container:create')
  //   expect(stdout).to.contain('hello world')
  // })

  // it('runs container:create --name oclif', async () => {
  //   const {stdout} = await runCommand('container:create --name oclif')
  //   expect(stdout).to.contain('hello oclif')
  // })
});
